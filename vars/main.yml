---
# vars file for ansible.motd
mylogofile: "files/logo.txt"
mylogo:  "{{ lookup('file', mylogofile ) }}"
myenvfile: "files/{{motd_ascii_art_with_environment}}.txt"
myenv: "{{ lookup('file', myenvfile ) }}"
banner: "{{ mylogo + myenv }}"
mybannerfile: "files/banner.txt"
